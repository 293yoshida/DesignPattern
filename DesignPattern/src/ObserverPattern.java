import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class ObserverPattern {
	@SuppressWarnings("unused")
	private static final Logger log = LoggerFactory
			.getLogger(ObserverPattern.class);
}
