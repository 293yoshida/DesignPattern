import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class MediatorPattern {
	@SuppressWarnings("unused")
	private static final Logger log = LoggerFactory
			.getLogger(MediatorPattern.class);
}
