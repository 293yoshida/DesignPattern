import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class ProxyPattern {
	@SuppressWarnings("unused")
	private static final Logger log = LoggerFactory
			.getLogger(ProxyPattern.class);
}
