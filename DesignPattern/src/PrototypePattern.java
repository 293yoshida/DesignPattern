import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class PrototypePattern {
	@SuppressWarnings("unused")
	private static final Logger log = LoggerFactory
			.getLogger(PrototypePattern.class);
}
